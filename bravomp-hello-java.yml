# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- master

pool:
  name: default
  # Used for Azure cloud
  #vmImage: 'ubuntu-latest'
variables:
  - name: project
    value: 'bravomp-hello-java'

steps:
# - task: Maven@3
#   inputs:
#     mavenPomFile: 'pom.xml'
#     mavenOptions: '-Xmx3072m'
#     javaHomeOption: 'JDKVersion'
#     jdkVersionOption: '1.8'
#     jdkArchitectureOption: 'x64'
#     publishJUnitResults: true
#     testResultsFiles: '**/surefire-reports/TEST-*.xml'
#     goals: 'package'
- task: CmdLine@2
  inputs:
    script: |
      echo Write your commands here
      polaris -v
      set PATH=C:\Polaris\polaris_cli-win64-1.6.99\bin;%PATH%
      echo %PATH%
      polaris -v
      C:\Users\mbravo\Documents\Polaris\sipse\polaris_cli-win64-1.6.99\bin\polaris -v
      where java
      where python
      where mvn
      echo Hello world
      curl -fLOsS $POLARIS_SERVER_URL/api/tools/polaris_cli-linux64.zip
      unzip -d $(Agent.TempDirectory) -jo polaris_cli-linux64.zip
      $(Agent.TempDirectory)/polaris --co project.name=$(PROJECT) analyze -w -- mvn -B package -DskipTests

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      # Write your PowerShell commands here.
      
      Write-Host "Hello World"